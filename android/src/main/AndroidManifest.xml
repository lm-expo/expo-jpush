<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- JPush requires app-specific permission to receive messages -->
  <permission
    android:name="${applicationId}.permission.JPUSH_MESSAGE"
    android:protectionLevel="signature" />
  <uses-permission android:name="${applicationId}.permission.JPUSH_MESSAGE" />

  <application>
    <!-- Required: service for core -->
    <service
      android:name="cn.jpush.android.service.JCommonService"
      android:enabled="true"
      android:exported="false" />

    <!-- Required: message service -->
    <service
      android:name="cn.jpush.android.service.PushService"
      android:enabled="true"
      android:exported="false" />

    <!-- Optional/compat: background keep-alive -->
    <service
      android:name="cn.jpush.android.service.DaemonService"
      android:enabled="true"
      android:exported="false" />

    <!-- Optional: download service -->
    <service
      android:name="cn.jpush.android.service.DownloadService"
      android:enabled="true"
      android:exported="false" />

    <!-- Required: provider -->
    <provider
      android:name="cn.jpush.android.service.DataProvider"
      android:authorities="${applicationId}.DataProvider"
      android:exported="false"
      android:grantUriPermissions="true" />

    <!-- JPush callback receiver (JPush SDK 5.9.x uses JPushMessageReceiver, not a Service) -->
    <receiver
      android:name="expo.modules.jpush.ExpoJPushMessageReceiver"
      android:enabled="true"
      android:exported="false"
      android:permission="${applicationId}.permission.JPUSH_MESSAGE">
      <intent-filter>
        <action android:name="cn.jpush.android.intent.RECEIVE_MESSAGE" />
        <category android:name="${applicationId}" />
      </intent-filter>
    </receiver>

    <receiver
      android:name="expo.modules.jpush.ExpoJPushBroadcastReceiver"
      android:exported="true"
      android:permission="${applicationId}.permission.JPUSH_MESSAGE">
      <intent-filter>
        <action android:name="cn.jpush.android.intent.REGISTRATION" />
        <action android:name="cn.jpush.android.intent.MESSAGE_RECEIVED" />
        <action android:name="cn.jpush.android.intent.NOTIFICATION_RECEIVED" />
        <action android:name="cn.jpush.android.intent.NOTIFICATION_OPENED" />
        <action android:name="cn.jpush.android.intent.CONNECTION" />
        <category android:name="${applicationId}" />
      </intent-filter>
    </receiver>
  </application>
</manifest>
